buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
    dependencies {
        // Updated to latest stable Gradle version
        classpath 'com.android.tools.build:gradle:8.2.0'
    }
}